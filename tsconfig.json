{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",

    /* Test matchers — augments Vitest's `Assertion` interface with jest-dom's
       DOM matchers (toBeInTheDocument, etc.) for every *.test.tsx under src/,
       since vitest.setup.ts itself lives outside src/ and tsc never sees it. */
    "types": ["@testing-library/jest-dom/vitest"],

    /* Linting */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,

    /* Paths — TS7 removed baseUrl; paths must be relative. Runtime resolution
       of the `src` alias is handled by Vite's own resolve.alias. */
    "paths": {
      "src/*": ["./src/*"]
    }
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
