# Build output
dist/
dist-ssr/
# `go build ./...`/`go vet ./...` from the repo root drops a compiled
# binary here named after each `package main` dir it finds — cmd/seed-demo
# included. Build/run it explicitly (`go build ./cmd/seed-demo`, `go run
# ./cmd/seed-demo`) if you want the binary; either way it doesn't belong in
# the repo.
/seed-demo
/fatura-cloud

# Frontend
node_modules/
package.json.md5

# Environment variables
.env
.env.local
.env.*.local

# Locales (compiled)
src/locales/*.mo
src/locales/*.js

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Sentry
.env.sentry-build-plugin

# Playwright screenshots/snapshots
.playwright-mcp/

# db/templates/gen's own output — the canonical file is db/templates/*.xlsx,
# committed separately after running `go run ./db/templates/gen`
db/templates/gen/*.xlsx
